# Copy to .env before: docker compose up -d
# Panel settings (no Steam password here — that goes in the bot .env)

NODE_ENV=production
API_KEY=
STEAM_AUTH=true
PORT=3000
SSL=false
VPS=false
ADDRESS=localhost
SESSION_SECRET=change-me-to-a-long-random-string
